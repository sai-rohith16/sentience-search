[build-system]
requires = ["setuptools>=61", "pybind11>=2.11"]
build-backend = "setuptools.build_meta"

[project]
name = "sentience-search"
version = "0.1.0"
description = "Movie vector KNN search (C++ extension)"
